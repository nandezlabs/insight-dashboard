name: insight_ui
description: Shared UI components and themes for Insight apps
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # Shared core package
  insight_core:
    path: ../insight_core

  # State Management
  flutter_riverpod: ^2.6.1

  # Charts
  fl_chart: ^0.69.2

  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
