name: insight_core
description: Shared business logic, models, and services for Insight apps
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Supabase

  # Models & Serialization
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Utils
  uuid: ^4.5.1
  intl: ^0.19.0

  # Local Storage
  drift: ^2.20.3
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.0

  # Security
  flutter_secure_storage: ^9.2.2
  encrypt: ^5.0.3
  crypto: ^3.0.5

  # Location
  geolocator: ^13.0.2

  # Weather
  weather: ^3.1.1

  # File handling
  file_picker: ^8.1.6
  csv: ^6.0.0
  excel: ^4.0.6
  dio: ^5.9.0

  # Device Info
  device_info_plus: ^11.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  riverpod_generator: ^2.6.2
  drift_dev: ^2.20.3
  flutter_lints: ^5.0.0
